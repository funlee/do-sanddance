import { Data, Transforms } from 'vega-typings';
import { Insight, SpecContext } from '../types';
export default function (context: SpecContext): Data[];
export declare function treemapTransforms(insight: Insight): Transforms[];
